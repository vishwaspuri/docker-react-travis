sudo: required 
services:
    - docker

before-install:
    - docker build -t vishwaspuri/docker-react-travis -f Dockerfile.dev .
    
scripts:
    - docker run vishwaspuri/docker-react-travis npm run test -- --coverage    
